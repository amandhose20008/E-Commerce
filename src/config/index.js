const config = {
    NAME_KEY: 'your-app-name', // Adjust with your app name
    DEFAULT_LANGUAGE: 'en', // Default language
    NODE_ENV: process.env.NODE_ENV || 'development', // Environment
    // Add more configuration options as needed
  };
  
  export default config;                    
  