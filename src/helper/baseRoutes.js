// Object containing base routes for user and admin sections
export const baseRoutes = {
  // Base route for the user section
  userBaseRoutes: "/",
  // Base route for the admin section
  adminBaseRoutes: "/admin",
};
  
// Comment: The above code defines a constant object named 'baseRoutes' that holds
// the base routes for the user and admin sections of the application. These routes
// can be used to navigate within their respective sections.
